'use strict';

/**
 * local-table-column router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::local-table-column.local-table-column');
