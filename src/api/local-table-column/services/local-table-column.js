'use strict';

/**
 * local-table-column service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::local-table-column.local-table-column');
